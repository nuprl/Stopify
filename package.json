{
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build:stopify": "cd stopify/ && yarn run build && cd -",
    "build:stopify-continuations": "cd stopify-continuations/ && yarn run build && cd -",
    "build:stopify-estimators": "cd stopify-estimators/ && yarn run build && cd -",
    "build": "yarn run build:stopify-continuations && yarn run build:stopify-estimators && yarn run build:stopify",
    "link:stopify": "cd stopify && ln -s `pwd`/. `npm -g root`/stopify",
    "link:stopify-continuations": "cd stopify-continuations && ln -s `pwd`/. `npm -g root`/stopify-continuations",
    "link:stopify-estimators": "cd stopify-estimators && ln -s `pwd`/. `npm -g root`/stopify-estimators",
    "link:all": "npm run link:stopify && npm run link:stopify-continuations && npm run link:stopify-estimators",
    "tslint:stopify": "cd stopify && yarn run tslint && cd -",
    "tslint:stopify-continuations": "cd stopify-continuations && yarn run tslint && cd -",
    "tslint:stopify-estimators": "cd stopify-estimators && yarn run tslint && cd -",
    "tslint-fix:stopify": "cd stopify && yarn run tslint-fix && cd -",
    "tslint-fix:stopify-continuations": "cd stopify-continuations && yarn run tslint-fix && cd -",
    "tslint-fix:stopify-estimators": "cd stopify-estimators && yarn run tslint-fix && cd -",
    "tslint": "yarn run tslint:stopify && yarn run tslint:stopify-continuations && yarn run tslint-fix:stopify-estimators",
    "tslint-fix": "yarn run tslint-fix:stopify && yarn run tslint-fix:stopify-continuations && yarn run tslint-fix:stopify-estimators"
  },
  "workspaces": [
      "stopify",
      "stopify-continuations",
      "stopify-estimators",
      "stopify-module-test"
  ]
}
